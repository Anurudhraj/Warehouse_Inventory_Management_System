"""Settings package.

Pick a module via ``DJANGO_SETTINGS_MODULE``:

* ``config.settings.development`` — local development (default)
* ``config.settings.production``  — docker-compose / deployed environments
* ``config.settings.test``        — automated tests
"""
